name: Deploy Fuzzer

permissions:
  contents: write
  actions: read

on:
  workflow_dispatch:

jobs:
  fuzzer-network:
    uses: ./.ci/deploy-component.yml
    with:
      env: fuzzer
      component: network
    secrets: inherit

  fuzzer-compute:
    needs: fuzzer-network
    uses: ./.ci/deploy-component.yml
    with:
      env: fuzzer
      component: compute
    secrets: inherit
